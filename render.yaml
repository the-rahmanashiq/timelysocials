services:
  - type: web
    name: timely-socials-api
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm install && npm run build:api
    startCommand: npm run start:api
    healthCheckPath: /health
    autoDeploy: true
